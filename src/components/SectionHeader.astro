---
interface Props {
	eyebrow?: string;
	title: string;
	lede?: string;
}

const { eyebrow, title, lede } = Astro.props;
---

<header>
	{eyebrow && <p class="eyebrow">{eyebrow}</p>}
	<h2>{title}</h2>
	{lede && <p class="lede">{lede}</p>}
</header>
