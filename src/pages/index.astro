---
import { getCollection } from 'astro:content';
import ButtonLink from '../components/ButtonLink.astro';
import { profile } from '../data/profile';
import Layout from '../layouts/Layout.astro';

const projects = (await getCollection('projects')).sort((a, b) => a.data.order - b.data.order);
const highlightProjects = projects.filter((project) => project.data.featured).slice(0, 3);
---

<Layout title="Shawn Guo - Software Engineer" description={profile.tagline}>
	<section class="home-readme">
		<div class="container">
			<p class="eyebrow">welcome.md</p>
			<h1>Hello there!</h1>
			<div class="home-about-copy">
				<p>
					I'm Shawn Guo, a software engineer specializing in systems, gameplay, and audio
					programming.
				</p>
				<p>
					I recently graduated from <strong>Johns Hopkins University</strong> with a B.S. in
					Computer Science and from the Peabody Institute's Music for New Media program with
					a B.M. in Music for New Media.
				</p>
				<p>
					I previously worked as a C++ Software Engineering Intern on the <i>Call of Duty</i> franchise
					at <strong>Activision / Raven Software</strong>, where I worked on runtime audio
					systems, tooling, and production audio pipelines.
				</p>
				<div class="home-logo-row" aria-label="Activision and Raven Software logos">
					<img src="/assets/activision-logo.png" alt="Activision" />
					<img src="/assets/raven-logo.png" alt="Raven Software" />
				</div>
				<p>
					Currently, I'm working as Lead Engineer with an audio focus at <strong>Snow Day Games</strong>
					on <em>Vesuvius</em>, a $15k funded action-adventure game, building interactive audio
					features that support exploration, combat, ambience, and player feedback.
				</p>
				<a
					class="vesuvius-cover-link"
					href="https://store.steampowered.com/app/4557830/Vesuvius/"
					aria-label="Open Vesuvius on Steam"
				>
					<img src="/assets/vesuvius-cover.png" alt="Vesuvius key art" />
				</a>
				<p>
					I'm interested in spatial audio, games programming, and DSP programming for purposes
					of immersive audio in games and interactive media.
				</p>
			</div>
		</div>
	</section>

	<section class="section home-section">
		<div class="container home-stack">
			<div class="home-block home-block--first">
				<div class="home-block__heading">
					<div>
						<p class="eyebrow">Project highlights</p>
						<h2>Three starting points.</h2>
					</div>
					<ButtonLink href="/projects">All Projects</ButtonLink>
				</div>
				<div class="mini-project-grid">
					{highlightProjects.map((project) => (
						<a class="mini-project-card" href={`/projects/${project.id}`}>
							<img src={project.data.image} alt={project.data.imageAlt} />
							<div>
								<span>{project.data.subtitle}</span>
								<strong>{project.data.title}</strong>
								<p>{project.data.summary}</p>
							</div>
						</a>
					))}
				</div>
			</div>
		</div>
	</section>
</Layout>
